# todo

- [x] increase the route grid's vertical size so the bottom of the route-target buttons does not appear to be cut off
- [x] the "sources" panel on the left should swap positions with the "route" panel on the right.  further, it should not be visible except while a route-target button is selected and clicking out of a button should deselect the button, hiding the panel
- [x] rather than explicitly showing each CC value (CC 1, CC 2, CC 3, CC 4) in the selection menu, just show "CC" and if a CC is added, automatically label it based on how many CCs are currently in the route grid.  CC should be the first option in the selection menu
- [x] in the route-target buttons, allow the user to grab a sliding button from the left and right edges of the output visualizer bar to edit the range
- [x] remove the gestures from the route panel and add them as sources to the opposite panel
- [x] add "note" as an option in the selection menu just after "CC" and when a note route-target button is active, show the note stream options in the sources panel
